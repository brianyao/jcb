%h2 单词
%h3#word= @word.title
%h3#in_chinese= @word.in_chinese
- if @recall == 'yes'
  = button_to '真记得:D', '/ji'
  = button_to '其实记不得:(', '/ji'
- elsif @recall == 'no'
  = button_to '下次记得:p', '/ji'
- else
  = button_to 'something is wrong...'
