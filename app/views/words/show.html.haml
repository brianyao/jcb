%h2 单词编辑
= form_tag word_path(@word), :method => :put do
  = render :partial => "editing_form"
  %div
    = submit_tag 'update'
= button_to 'Delete', word_path(@word), :method => :delete, :confirm => 'Are you sure?'
